# Only needed to preview the site locally; GitHub Pages builds it server-side.
#   bundle install && bundle exec jekyll serve
# Requires Ruby >= 3.0 (macOS system Ruby 2.6 is too old: `brew install ruby`).
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
